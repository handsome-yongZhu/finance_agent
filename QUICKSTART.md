# 🚀 10 分钟快速部署到公网

> 最简单的方式：腾讯云轻量服务器（￥74/年）

---

## 第一步：购买服务器（2分钟）

1. 访问 **[腾讯云轻量应用服务器](https://cloud.tencent.com/product/lighthouse)**
2. 选择配置：
   - **地域**：选择离你最近的（如：上海、北京、广州）
   - **镜像**：Ubuntu 22.04 或 Docker
   - **套餐**：2核2G（￥74/年）
   - **流量包**：1000GB/月（足够用）
3. 点击"立即购买"，完成付款

**💡 提示**：腾讯云新用户有更多优惠，首年可能更便宜

---

## 第二步：连接服务器（1分钟）

### 方式一：使用腾讯云网页 Shell（最简单）

1. 进入 **轻量应用服务器控制台**
2. 找到你的服务器，点击"登录"
3. 选择"立即登录"（自动打开网页终端）

### 方式二：使用 SSH（推荐）

```bash
# 在你的电脑上执行（Mac/Linux 自带，Windows 用 PowerShell）
ssh root@你的服务器IP

# 首次连接需要输入密码（在腾讯云控制台重置）
```

---

## 第三步：一键部署（5分钟）

### 🎯 超简单：一条命令搞定

```bash
# 复制粘贴这条命令，回车
curl -fsSL https://raw.githubusercontent.com/你的用户名/Finance-Agent/main/deploy-cloud.sh | bash
```

如果上面的命令失败（GitHub 访问慢），使用手动方式：

### 📝 手动部署（同样简单）

```bash
# 1. 安装 Docker（1分钟）
curl -fsSL https://get.docker.com | sh
systemctl start docker
systemctl enable docker

# 2. 克隆项目（30秒）
git clone https://github.com/MiniMax-AI/Mini-Agent.git
cd Mini-Agent

# 3. 配置 API Key（1分钟）
cp mini_agent/config/config-example.yaml mini_agent/config/config.yaml
vim mini_agent/config/config.yaml

# 按 i 进入编辑模式，填入你的 API Key：
# api_key: "你的API密钥"
# api_base: "https://api.minimaxi.com"
# 
# 确保启用了 MCP 工具：
# tools:
#   enable_mcp: true
#
# 按 ESC，输入 :wq 保存退出

# 4. 启动服务（30秒）
docker-compose up -d

# 5. 查看服务状态
docker-compose ps
```

---

## 第四步：开放端口（1分钟）

1. 进入 **腾讯云轻量应用服务器控制台**
2. 点击你的服务器 → **防火墙**
3. 点击 **添加规则**：
   - **应用类型**：自定义
   - **协议**：TCP
   - **端口**：8080
   - **策略**：允许
4. 点击 **确定**

---

## 第五步：访问系统（1分钟）

打开浏览器，访问：

```
http://你的服务器IP:8080
```

🎉 **恭喜！你的金融报告系统已经上线了！**

---

## 🔍 验证部署

### 1. 检查服务状态

```bash
docker-compose ps
```

应该看到两个服务都是 `Up` 状态：
- `financial-reporter-web` - Web 服务器
- `financial-reporter-scheduler` - 定时调度器

### 2. 查看日志

```bash
# 查看所有日志
docker-compose logs -f

# 只看 Web 服务日志
docker-compose logs -f web

# 只看调度器日志
docker-compose logs -f scheduler
```

### 3. 测试报告生成

```bash
# 手动触发一次报告生成（测试）
docker-compose exec scheduler python financial_reporter.py --reports-dir /app/reports
```

几分钟后，应该能在 Web 界面看到生成的报告。

---

## 📱 使用系统

### 访问 Web 界面

在浏览器打开：`http://你的服务器IP:8080`

你会看到：
- 📊 **首页**：报告列表和统计信息
- 📄 **报告详情**：点击"查看报告"阅读完整内容
- 📥 **下载报告**：下载 Markdown 格式原文件

### 自动生成报告

系统会在每天 **9:30**（开盘后）自动生成报告。

**修改执行时间**：

```bash
# 编辑 docker-compose.yml
vim docker-compose.yml

# 找到这一行并修改时间
- SCHEDULE_TIME=09:30  # 改成你想要的时间

# 重启服务
docker-compose restart scheduler
```

---

## ⚙️ 配置股票列表

系统默认追踪：`688388, 601318, 601212, 688091`

**首次生成**：完整详细的初始报告  
**后续每天**：简洁的增量更新报告

如需修改股票列表，编辑 `financial_reporter.py`：

```bash
vim financial_reporter.py

# 找到这一行，修改股票代码
stock_codes = ["688388", "601318", "601212", "688091"]
```

---

## 🔧 常用命令

```bash
# 查看服务状态
docker-compose ps

# 查看日志（实时）
docker-compose logs -f

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 启动服务
docker-compose up -d

# 进入容器
docker-compose exec web bash

# 手动生成报告
docker-compose exec scheduler python financial_reporter.py --reports-dir /app/reports
```

---

## 🌐 配置域名（可选）

如果你有域名，可以配置更好看的访问地址。

### 1. 域名解析

在你的域名服务商（阿里云、腾讯云等）添加 A 记录：

```
类型: A
主机记录: finance (或 @ 代表根域名)
记录值: 你的服务器IP
TTL: 600
```

### 2. 配置 Nginx

```bash
# 安装 Nginx
apt install nginx -y

# 创建配置文件
cat > /etc/nginx/sites-available/finance << 'EOF'
server {
    listen 80;
    server_name finance.你的域名.com;
    
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
EOF

# 启用配置
ln -s /etc/nginx/sites-available/finance /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```

### 3. 配置 HTTPS（推荐）

```bash
# 安装 Certbot
apt install certbot python3-certbot-nginx -y

# 自动配置 HTTPS
certbot --nginx -d finance.你的域名.com

# 自动续期
certbot renew --dry-run
```

现在可以通过 `https://finance.你的域名.com` 访问！

---

## 🐛 常见问题

### Q: 无法访问 Web 界面？

**检查清单**：
1. ✅ 服务器防火墙是否开放 8080 端口？
2. ✅ 云服务商安全组是否开放 8080 端口？
3. ✅ Docker 容器是否正常运行？（`docker-compose ps`）
4. ✅ 服务是否启动成功？（`docker-compose logs web`）

```bash
# 测试本地是否能访问
curl http://localhost:8080/health

# 如果本地可以访问，说明是防火墙问题
```

### Q: 报告生成失败？

**检查清单**：
1. ✅ API Key 配置正确？
2. ✅ MCP 工具已启用？
3. ✅ 网络可以访问 MiniMax API？

```bash
# 查看详细错误日志
docker-compose logs scheduler

# 手动测试一次
docker-compose exec scheduler python financial_reporter.py --reports-dir /app/reports
```

### Q: 定时任务不执行？

```bash
# 检查调度器是否运行
docker-compose ps scheduler

# 查看调度器日志
docker-compose logs scheduler

# 检查时区设置
docker-compose exec scheduler date
```

### Q: 想要停止服务？

```bash
# 停止服务（但保留数据）
docker-compose down

# 停止并删除所有数据
docker-compose down -v
```

---

## 💰 费用明细

### 最低配置（个人使用）

- **云服务器**：￥74/年（腾讯云轻量 2核2G）
- **流量**：包含 1000GB/月（够用）
- **域名**：￥9-55/年（可选）
- **总计**：￥74-129/年

### 平均每天不到 2 毛钱！☕

---

## 📈 下一步

1. **设置通知**：报告生成后自动发送到邮箱或微信
2. **添加更多股票**：追踪更多你关心的股票
3. **自定义报告**：修改报告内容和格式
4. **数据可视化**：添加图表展示

详细配置请参考：[DEPLOYMENT.md](DEPLOYMENT.md)

---

## 🆘 需要帮助？

- 📖 详细文档：[DEPLOY_CLOUD.md](DEPLOY_CLOUD.md)
- 💬 提交 Issue：[GitHub Issues](https://github.com/MiniMax-AI/Mini-Agent/issues)
- 📧 联系我们：参考 README.md

---

🎉 **恭喜！你已经成功部署了自己的金融报告系统！**

现在每天都能自动收到专属的股票分析报告了！📊✨
